---
description: Thicken one harness pillar beyond its thin onboarding layer — co-built with the human, one pillar at a time.
argument-hint: [pillar name — e.g. "mechanical enforcement", "runtime legibility"]
---

Read `docs/harness/workflows/pillar.md` and follow it exactly. That file is the single source of truth for this workflow; this command is only a Claude Code entry point to it. If `$ARGUMENTS` names a pillar, thicken that one.
